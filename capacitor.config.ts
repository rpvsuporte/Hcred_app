import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.hcred.hcredApp',
    appName: 'HCred - Soluções Financeiras',
    webDir: 'www'
};

export default config;
